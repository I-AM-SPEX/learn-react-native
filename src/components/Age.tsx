import { View, Text } from "react-native";
import React from "react";
import styles from "../utils/style";

const Age = () => {
  return (
    <View style={styles.textContainer}>
      <Text>I'm 2000 yers old.</Text>
    </View>
  );
};

export default Age;
