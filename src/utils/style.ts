import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textContainer: {
    padding: 20,
    margin: 20,
    shadowColor: "black",
    shadowOpacity: 0.5,
    shadowOffset: {
      width: 6,
      height: 6,
    },
    shadowRadius: 3,
    borderWidth: 1,
    borderRadius: 3,
  },
});

export default styles;
